import React from "react";
import { useSelector } from "react-redux";

function Main() {
  //   const { todos } = useSelector((state) => state.todo);
  //   console.log(todos);

  return <div>Main</div>;
}

export default Main;
